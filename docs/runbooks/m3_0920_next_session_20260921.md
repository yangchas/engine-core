# M3-1 09:20 正常来源验证运行单

状态：`PENDING_NEXT_TRADING_SESSION`

本运行单不是回放，也不是生产替代。它只用于下一交易日（当前日历快照确认的
`2026-09-21`）在 `cobra-ion` 上执行一次真实 Redis 只读旁路。

## 当前边界

- `engine-next`、`t1-v2-live` 保持运行。
- 不新增 Rabbit consumer，不改变 ACK。
- 不写 Redis/TD，不发送邮件、通知或任何 effect。
- 不允许用盘后 `RECOVERY_CATCHUP` 结果替代本次 `NORMAL` 验收。
- 本次只验证 `AUCTION_0920`，通过后才安排 09:24/09:25。

## 开盘前只读预检

```bash
date -Is
systemctl is-active engine-next t1-v2-live
df -h / /home/exedev
test -r /home/exedev/validation/cc-m0-calendar-20260911-v3.json
```

盘前还要检查上一交易日是否出现 TD 提交空间错误：

```bash
if journalctl -u t1-v2-live --since '2026-09-18 00:00:00' --no-pager \
  | grep -q 'stage=commit.tdengine.*No enough disk space'; then
  echo 'TD_WRITE_HEALTH=BLOCKED'
  # Redis-only Q2 观测可另行记录，但不得将 TD 作为完整 ground truth。
else
  echo 'TD_WRITE_HEALTH=NO_KNOWN_DISK_ERROR'
fi
journalctl -u t1-v2-live --since '09:15:00' --no-pager \
  | grep 't1_v2 progress' | tail -1
```

发现空间错误时不执行删除、prune、keep 变更或服务重启；本运行单只允许记录
`BLOCKED/WARN`，等待单独批准的容量/保留策略处置。
受控窗口内没有新的 progress/heartbeat 时，同样不得把 systemd `active` 解释为
实时消费健康。

再用同一远端 Python 运行时确认目标交易日实际存在于快照，不只检查文件存在：

```bash
/home/exedev/services/engine-next/shared/venv/bin/python - <<'PY'
import json

path = "/home/exedev/validation/cc-m0-calendar-20260911-v3.json"
trade_date = "2026-09-21"
payload = json.load(open(path, encoding="utf-8"))
dates = payload.get("trading_dates", ())
if trade_date not in dates:
    raise SystemExit(f"calendar does not contain target trade date: {trade_date}")
print(f"calendar_target_present={trade_date}")
PY
```

若服务不是 `active`、日历文件不可读、目标交易日不在快照、磁盘空间不足，或当前时间不在 09:15
至 09:21 的受控窗口内，停止并记录 `BLOCKED`，不使用恢复或 fallback 路径。

## 09:20 运行命令

在远端已验证副本中执行。本运行单固定使用
`ae30a84e4342b4ba5a087919ff2539ae0b82ae7` 对应的验证归档；该副本是归档展开目录，
不是 Git 工作树。执行前必须校验归档 SHA-256 与目录及脚本存在。`observed_at` 与
`as_of` 必须由同一次远端时钟读取，不能手填盘中时间：

```bash
cd /home/exedev/validation/engine_core-ae30a84
test "$(sha256sum /home/exedev/validation/engine_core-ae30a84.tar | awk '{print $1}')" \
  = "912be37c92fff44b018bc51e4bd8fa7aaa06388729cbc0f92bd6156f26e9fdb0" \
  || { echo 'verified archive SHA mismatch' >&2; exit 1; }
test -f examples/run_m3_0920_shadow.py \
  || { echo 'verified copy missing M3-1 runner' >&2; exit 1; }
OBSERVED_AT="$(date -Is)"
/home/exedev/services/engine-next/shared/venv/bin/python \
  examples/run_m3_0920_shadow.py \
  --trade-date 2026-09-21 \
  --symbol 000338 \
  --calendar-file /home/exedev/validation/cc-m0-calendar-20260911-v3.json \
  --observed-at "$OBSERVED_AT" \
  --as-of "$OBSERVED_AT" \
  --stale-after-ms 10000 \
  --origin NORMAL \
  --output "/home/exedev/validation/m3-0920-shadow-20260921-normal-000338.json"
```

## 正常路径验收

只有以下条件全部满足，才记录 `M3_1_NORMAL=PASS`：

- `origin=NORMAL`。
- `startup_self_check.status=READY`。
- 交易日、时区、日历 hash、session plan hash 一致。
- Q2 只预取一次；coverage、freshness、completeness 原样保留。
- 0920 projection 存在且只读可解析。
- `preflight_gate=PASS`、`node_dispatched=true`。
- Engine 只创建一个实例，且只消费本次 0920 输入。
- 输出保持事实/探针语义，不产生策略结论或 effect。
- 输出文件 write-once，记录 SHA-256。

任何一项不满足，都记录具体 `BLOCKED`/`WARN` 原因；不得改阈值、补字段或把
`RECOVERY_CATCHUP` 改标成 `NORMAL`。

## 运行后证据

记录：

```text
commit SHA
remote Python version
calendar manifest SHA
runner SHA
output SHA-256
Q2 content/source-time range
prefetch_calls
preflight_gate
node_dispatched
read/write/effect counters
```

当天只提交证据和文档。若 M3-1 正常路径通过，下一步再单独设计 09:24；若未通过，
先修复该门禁，不扩展节点和策略。

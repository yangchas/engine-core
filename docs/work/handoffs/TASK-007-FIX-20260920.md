# TASK-007 acceptance reopening and first repair pass

审计结论：原 TASK-007 integrator review 只证明了测试集通过，未证明降级
frame 证据和竞价软截止时序完整。2026-09-20 14:46 +08:00 起重新打开
TASK-007 验收；本修复仍只涉及纯 Core/offline seam。

## 已修复

- 全部 tick 缺失必需字段时保留 `BLOCKED` frame 诊断，不中断后续 3 秒时间线。
- `replay()` 将 `BLOCKED/PARTIAL/EMPTY`、跳过 symbol、原因、batch quality、
  source/order metadata 传入 Engine projection 和 reducer evidence。
- frame evidence/hash 不再丢弃 batch quality 与同毫秒顺序歧义。
- 空 canonical batch 按 `logical_ts_ms` 绑定到对应 frame，并保留
  `source_batch_id/source_sequence`；没有 source batch 的空 frame 保持
  completeness `UNKNOWN`。
- 冲突的 `historical_available_at_ms` 不会被合并成虚假的 `KNOWN` 时间。
- `OfflineCanonicalReplay` 可选接入匹配的 `ReplaySessionTimeline`，逐 frame
  记录并将 auction observation 写入同一纯内存 ledger；默认行为不变。
- 相同竞价内容在不同 evaluation time 观察时保持同一 content revision，
  但更新最新 timing/freeze evidence；迟到新内容仍创建新 revision。
- ReplaySessionTimeline 允许同一 source revision 更新 timing-derived node evidence。

## 当前验证

```text
pytest: 676 passed, 3 warnings
compileall: PASS
diff-check: PASS
production side effects: NONE_OBSERVED
```

## 尚未宣称完成

- Rabbit live arrival order、historical `available_at`、生产等价性仍 UNKNOWN。
- TASK-007 仍需独立复核缺失 batch 的恢复策略、500-frame 证据和完整 auction
  session timeline；不得据此解除 `M3_1_NORMAL=BLOCKED`。

# TASK-008 audit — real opening validation

审计时间：2026-09-20 18:45 +08:00  
审计类型：ECC production-audit 本地证据审计  
审计对象：TASK-008、当前 HEAD `eee158f`

## Verdict

```text
TASK_008_AUDIT=PARTIAL_EVIDENCE
TASK_008_MERGE=HOLD
NORMAL_OPENING_PASS=UNPROVEN
PRODUCTION_SIDE_EFFECTS=NONE_OBSERVED
```

TASK-008 的只读代码路径和盘后真实观测执行成功，但尚未达到“正常
09:32:10 opening validation”或 `MERGED` 条件。

## Passed checks

- 真实 Redis Q2 通过既有 `SMEMBERS/HGETALL` 读取；没有 Redis 写入、TD
  写入、Rabbit consume/ACK、服务重启或 effect。
- `projection_status=PARTIAL`、`freshness_status=STALE_OR_MIXED`、缺失和
  stale 数量被显式保留，未将缺失补成零。
- 一个 in-memory Engine 处理 2 个 signal，输出
  `FACT_ONLY` / `OBSERVE`，没有策略交易结论。
- 相关 opening/Q2/canonical/session 测试：`96 passed`；compileall 和
  `git diff --check` 通过；工作树干净。
- Core 源码没有 Redis、TDengine、Rabbit client/writer 或 effect import；
  外部 client 仅存在于明确的 real-data example 边界。

## Blocking findings for closure

1. **没有正常时段证据**：本次 observation 是 `2026-09-20 18:35 +08:00`
   的盘后读取，不是 `2026-09-18 09:32:10` 或下一交易日受控窗口。
2. **覆盖不足**：实际只得到 `1000 / 5224` quotes，coverage
   `0.19142419601837674`；`4224` symbols missing，`1000` stale。
3. **历史 cutoff 未证明**：`historical available_at` 未知，源时间范围也不
   能证明这些值在 09:32:10 已可用。因此不得把本次结果解释为 NORMAL
   opening replay 或生产等价性。

## Non-blocking findings

- `run_real_opening_facts.py` 的 CLI 使用当前 `datetime.now()` 作为观察时间，
  不支持从命令行重放指定历史 observation cutoff；若要做可复现的历史
  09:32:10 验证，应增加显式 `--observed-at` 或使用已冻结的带 availability
  evidence 输入。
- 本次 validation 目录有摘要和副作用记录，但没有统一 `sha256sums.txt`；
  后续受控证据应补齐 manifest/checksum。

## Evidence checked

```text
/home/exedev/validation/task008-opening-validation-20260920T183000+0800/
```

重点文件：`opening_facts.json`、`opening_engine_000001.json`、
`validation_summary.json`、`side_effect_audit.json`。实测摘要为：

```text
projection=PARTIAL / BEST_EFFORT_PARTIAL
freshness=STALE_OR_MIXED
expected=5224, observed=1000, missing=4224, stale=1000
engine=2 signals / PARTIAL / FACT_ONLY / OBSERVE
```

## Required next action

在不修改生产 owner 的前提下，下一次受控交易窗口捕获 09:32:10 或更晚的
真实 observation，记录实际 source time、observed/evaluation time、
availability/cutoff 证据和完整性；如果仍为 stale/partial，继续输出
`PARTIAL/UNPROVEN`，不要为了通过门禁放宽 freshness 或补造数据。

`M3_1_NORMAL=BLOCKED`、`TD_WRITE_HEALTH=UNPROVEN` 保持不变。

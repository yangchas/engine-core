# Agent Handoffs

Each role writes one task-scoped handoff at:

```text
docs/work/handoffs/<task-id>-<role>.md
```

Handoffs must contain status, changed files, commands, evidence paths,
unknowns, side-effect counters, and the next owner. Handoffs are not a license
to execute the next card automatically.

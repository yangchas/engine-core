# Plans

Store planner handoffs here as `docs/work/plans/<task-id>.md`. A plan must
name its owner, authorized files, commands, evidence, merge gate, unknowns,
and blocked conditions before a card leaves `READY`.
